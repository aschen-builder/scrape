"use strict";

const { expect } = require('chai');

const snews = require('../src/sites/sneakernews');

describe('sneakernews.js', () => {
    describe('init()', () => {
        it('Initialize https://sneakernews.com release scraping service', () => {
             
        });
    });

    describe('scrape()', () => {

    });
});